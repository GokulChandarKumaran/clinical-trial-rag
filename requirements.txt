# Service
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Retrieval
faiss-cpu==1.9.0.post1
sentence-transformers==3.3.1

# Local generation backend (default; no API key required)
transformers==4.47.1
torch==2.5.1

# Optional backends — imported lazily, so absence only breaks that backend
# openai==1.59.0
